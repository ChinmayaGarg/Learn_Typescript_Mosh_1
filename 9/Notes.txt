In TS, we have a new variable "any" which can represent any type of value.
If we declare any variable and didn't initialize it, TS compiler assumes  it is of type any.
This is against the whole idea of using TS. We use TS for type-safety, so we get type checking.